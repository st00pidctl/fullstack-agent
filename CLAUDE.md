# Claude Code compatibility shim

This repository is no longer architecturally Claude-specific.

Read `AGENTS.md` first. It is the canonical instruction file for this fork.

When the user asks to set up this repository, also read `fullstack-agent.md`, but interpret provider-specific instructions through the portable core model documented in `architecture/CORE_CONTRACT.md`.

Do not make Claude-specific behavior canonical when a provider-neutral equivalent can be used.
